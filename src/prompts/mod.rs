//! Pattern-specific kickoff/setup instruction text, injected directly into
//! agent panes at launch (as opposed to `devorch_get_setup_instructions`,
//! which serves the legacy team-role instructions over MCP).

pub mod fixbug;
